Make all Settings page buttons, switches, cards, and menu items fully functional. Do not leave any placeholder UI elements. Implement complete working functionality for every option shown on the Settings screen. Dark Mode should instantly switch between light and dark themes and persist the preference locally. Notifications should enable and disable application notifications and save the preference. Auto Sync should automatically synchronize pending attendance and worker registration records whenever connectivity becomes available.

Backup Database should create a complete local backup of workers, attendance records, operators, devices, toll plazas, settings, and synchronization logs, then save the backup file to device storage. Restore Backup should allow the user to select a backup file and restore all application data safely. Export CSV should generate attendance reports and worker records in CSV format and allow sharing or downloading.

About Application should display application version, build number, platform information, device information, developer information, legal information, and release notes. API Configuration should display current API endpoints, synchronization configuration, server status, and connectivity information. Offline Mode status should be automatically updated based on network availability.

Add Device Information showing Device ID, Device Token, Device Model, Platform, OS Version, Registration Date, Last Sync Time, Assigned Toll Plaza, and Assigned Operator. Add Sync Status showing pending records, successful records, failed records, and last synchronization timestamp. Add Clear Cache functionality that safely removes temporary files without affecting attendance or worker data.

All settings must be stored persistently using local storage and should remain active after application restart. Every button, toggle, export operation, backup operation, restore operation, and synchronization action should provide loading indicators, success messages, error handling, and confirmation dialogs. Ensure all settings functionality works correctly on Android and iOS devices with a professional enterprise-grade user experience.
